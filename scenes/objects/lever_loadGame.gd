extends Lever

func _on_AnimationPlayer_animation_finished(_anim_name):
	print("feature not complete")
	return
	#var data = Global.load_previous_save() 
#	if data != null: 
#		print("data loaded")
#		print(data)
#		Global.scrap = data["scrap_metal"]
#		Global.cores = data["energy_cores"]
#		Global.spawn_point = data["spawn_point"]
#	Global.player.set_deferred("position", Global.spawn_point.position)
